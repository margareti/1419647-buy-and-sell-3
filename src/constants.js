'use strict';

const MAX_LIMIT = 1000;
const DEFAULT_PORT = 3000;
const HTTP_SUCCESS_CODE = 200;
const NOT_FOUND_CODE = 404;

module.exports = {
  MAX_LIMIT,
  DEFAULT_PORT,
  HTTP_SUCCESS_CODE,
  NOT_FOUND_CODE,
};
